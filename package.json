{
  "name": "joe-coffee-blog",
  "version": "1.0.0",
  "description": "joe.coffee blog with Eleventy",
  "scripts": {
    "generate:locations": "node generate-locations.js",
    "build": "node generate-locations.js && node generate-api.js && eleventy",
    "serve": "eleventy --serve"
  },
  "dependencies": {
    "@11ty/eleventy": "^2.0.1"
  }
}
